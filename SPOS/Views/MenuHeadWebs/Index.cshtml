@model IEnumerable<SPOS.Models.MenuHeadWeb>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MenuHeadWebName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Priority)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DivID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MenuHeadWebName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Priority)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DivID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MenuHeadWebID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MenuHeadWebID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MenuHeadWebID })
        </td>
    </tr>
}

</table>
